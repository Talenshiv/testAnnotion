/** Типизированное значение формы создания и редактирования статьи. */
export interface ArticleFormValue {
  /** Заголовок статьи (обрезается при сохранении). */
  title: string;
  /** Тело статьи в виде обычного текста. */
  content: string;
}
